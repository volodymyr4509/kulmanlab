---
sidebar_position: 4
title: 文字编辑器 — KulmanLab CAD 中的富文本格式
description: KulmanLab CAD 文字编辑器可让您放置带有粗体、斜体、删除线、每字符字体和高度覆盖、自动换行和完整光标导航的多行、富格式 MTEXT 标签。
keywords: [CAD 文字编辑器, MTEXT, 粗体斜体 CAD, 文字格式 CAD, 多行文字 CAD, 自动换行 CAD, kulmanlab]
---

# 文字编辑器

文字编辑器在您使用 `text` 命令放置新文字标签或双击现有文字图元时打开。它支持多行内容、每字符格式和自动换行。

## 打开编辑器

| 操作 | 结果 |
|--------|--------|
| `text` 命令 → 单击位置 | 创建新文字图元并打开编辑器 |
| 双击现有文字图元 | 重新打开该图元的编辑器 |
| 编辑器内按 `Escape` | 关闭编辑器并保留所有更改 |

## 工具栏

工具栏浮动在文字边界框上方，随您平移或缩放保持锚定到图元。

### 粗体 · 斜体 · 删除线

| 按钮 | 快捷键 | 功能 |
|--------|----------|--------------|
| **B** | — | 切换粗体 |
| *I* | — | 切换斜体 |
| ~~S~~ | — | 切换删除线 |

**切换如何应用：**

- **有文字选择** — 样式仅应用于所选字符。
- **无选择，光标在现有文字中** — 切换整个图元（所有段）的样式。
- **空文字或新图元** — 样式存储在空段上，并应用于从该点起输入的每个字符。

当当前选择中的每个字符 — 或光标左侧的字符 — 都设置了该样式时，按钮显示为高亮（激活状态）。

### 字体

下拉菜单列出一组精选的跨平台字体（Helvetica、Times New Roman、Georgia、Courier New、Verdana、Tahoma、Trebuchet MS、Lucida Console、Impact）。顶部选项 **Arial** 为默认值。

- **有选择** — 仅覆盖所选字符的字体。
- **无选择** — 应用字体到整个图元。

下拉菜单反映无选择时光标左侧字符的字体。

### 高度

数字字段以图形单位设置**大写字母高度**（大写字母的高度）。

- **有选择** — 覆盖所选字符的高度，独立于图元的基础高度。
- **无选择** — 更改图元的基础高度（应用于所有没有单独高度覆盖的字符）。

字段反映光标左侧字符的高度。留空以使用图元默认值。

## 光标和导航

| 按键 | 操作 |
|-----|--------|
| `←` / `→` | 向左或向右移动插入符一个字符 |
| `Home` | 跳到当前硬换行的开头 |
| `End` | 跳到当前硬换行的结尾 |
| `Shift` + `←` / `→` | 扩展或缩小选择 |
| `Backspace` | 删除左侧字符（或选中内容） |
| `Delete` | 删除右侧字符（或选中内容） |
| `Enter` | 插入换行符 |
| `Escape` | 关闭编辑器 |

光标高度自动匹配相邻字符的大写字母高度，包括用于下标和上标的较小尺寸。

## 自动换行

当文字图元设置了**参考宽度**时，长行在词边界处软换行以适应该宽度。

在图元选中时设置或更改参考宽度，拖动**调整大小夹点** — 虚线边界框左右边缘上的细矩形。内容在拖动时实时重新排版。

将参考宽度设置为零（将夹点拖在一起或删除属性面板中的值）可删除自动换行并让行自由增长。

## 多行文字

按 `Enter` 插入硬换行。每条硬换行是独立的 — `Home` 和 `End` 仅在当前硬换行内导航。

硬换行和每字符格式使用 MTEXT 格式存储，在完整 DXF 往返转换后仍然存在。

## DXF 兼容性

文字图元在 DXF 文件中以 **MTEXT** 格式存储。粗体和斜体使用 `\L`、`\K`、`\O` 和内联字体切换（`\f`）编码。每字符高度使用 `\H` 编码。所有格式化在导出时保留，可被 LibreCAD、FreeCAD 和其他支持 DXF 的应用程序读取。
